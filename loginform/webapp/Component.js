sap.ui.define([
    "sap/ui/core/UIComponent"
], function (UIComponent) {
    "use strict";

    return UIComponent.extend("demo.loginfrom.loginform.Component", {
        metadata: {
            manifest: "json"
        }
    });
});